[project]
name = "hyperact-mcp-infra"
version = "0.1.0"
description = "Pulumi infrastructure for hyperact-mcp (Cloud Run + global HTTPS LB)"
requires-python = ">=3.10"
dependencies = [
  "pulumi>=3,<4",
  "pulumi-gcp>=7,<8",
  "pulumi-docker>=4,<5",
]

[tool.uv]
package = false
